From 2825084bfaaeccec6abb998bf331e2edaeecc704 Mon Sep 17 00:00:00 2001
From: Grigori Fursin <Grigori.Fursin@cTuning.org>
Date: Thu, 10 Nov 2016 16:02:29 +0100
Subject: [PATCH] support/hack for Android

---
 src/logging_unittest.cc | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/src/logging_unittest.cc b/src/logging_unittest.cc
index b886222..619415b 100644
--- a/src/logging_unittest.cc
+++ b/src/logging_unittest.cc
@@ -637,6 +637,9 @@ static void GetFiles(const string& pattern, vector<string>* files) {
   } while (FindNextFileA(handle, &data));
   BOOL result = FindClose(handle);
   LOG_SYSRESULT(result);
+#elif defined(ANDROID)
+// So far, Android NDK libc does not have glob function.
+# warning "This is a temporal workaround for compilation issue."
 #else
 # error There is no way to do glob.
 #endif
-- 
2.7.4

